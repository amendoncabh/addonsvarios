<?xml version="1.0"?>
<odoo>

    <!-- Dashboard -->

    <record id="inherit_waraporn_view_pos_config_kanban" model="ir.ui.view">
        <field name="name">inherit.waraporn.pos.config.kanban.view</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_config_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@class='col-xs-6 o_kanban_primary_right']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

</odoo>
